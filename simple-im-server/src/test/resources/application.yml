spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:simple_im
    username: sa
    password:
    schema: classpath:db.sql
mybatis-plus:
  global-config:
    db-config:
      logic-delete-value: 1
      logic-not-delete-value: 0
server:
  port: 8888
logging:
  level:
    ROOT: info
